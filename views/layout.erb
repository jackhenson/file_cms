<!doctype html>
<html lang="en-US">
  <body>
    
    <% if session[:message] %>
      <!-- Deletes and returns hash value -->
      <p><%= session.delete(:message) %></p>
    <% end %>
    
    <main>
      <%= yield %>
    </main>
  </body>
</html>
